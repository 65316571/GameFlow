{
  "name": "GameTracker",
  "version": "1.0.0",
  "description": "GameTracker - 游戏时间追踪系统",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd GameTracker-client && npm run dev",
    "dev:server": "cd GameTracker-server && npm run dev",
    "init-db": "cd GameTracker-server && npm run init-db",
    "install:all": "npm install && cd GameTracker-client && npm install && cd ../GameTracker-server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
